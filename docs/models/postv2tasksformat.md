# PostV2TasksFormat

The format of the task content to be created. Rich text formatting, links and @references are not supported.

## Example Usage

```python
from attio.models import PostV2TasksFormat
value: PostV2TasksFormat = "plaintext"
```


## Values

- `"plaintext"`
