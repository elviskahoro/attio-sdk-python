# TypeRecord

The type of entity tagged in the note. Can be either 'workspace-member' or 'record'

## Example Usage

```python
from attio.models import TypeRecord
value: TypeRecord = "record"
```


## Values

- `"record"`
