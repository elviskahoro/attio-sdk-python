# TokenType

The type of token, always Bearer for tokens acquired via the OAuth 2.0 flow.

## Example Usage

```python
from attio.models import TokenType
value: TokenType = "Bearer"
```


## Values

- `"Bearer"`
